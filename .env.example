# Frontend production URL, comma-separated if more than one origin.
CORS_ORIGINS=https://your-project.vercel.app

# Optional. The backend defaults to a local SQLite database.
# DATABASE_URL=sqlite:///./petadvice.db